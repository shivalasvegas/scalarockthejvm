package exercises

abstract class MyList {
    // head = first element of list
    // trail = remainder of list
    // isEmpty = is this list empty
    // add(int) => new list with element added
    // toString => a string representation of the list
  
}
